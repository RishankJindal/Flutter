//* Here we will be discussing particular data-types for variables.

void main() {
  // int age = 20;
  // double price = 30.70;
  // bool isChecked = false;
  // String name = "rishank";

  //? If we do not specify about the elements of List and Map, it will take it as dynamic by default.And we can change the data-type of its elements unline when we use 'var' keyword.
  // List items = ['apple', 'banana'];
  // Map m1 = {"key1": "value1", "key2": "value2"};

  // print(items);
  // items = [10, 20];
  // print(items);

  //? Now if we want to specify element type in List and Map, we can do so.
  // List<int> items2 = [10, 20, 30];
  // print(items2);

  // Map<String, int> m2 = {
  //   'age': 20,
  //   'exp': 5,
  //   'range': 120,
  // };

  // print(m2['age']);
}
